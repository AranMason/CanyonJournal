import { GearItem, RopeItem } from "../../src/types/types";

export const loadGearForDescent = (descentIds: number[]): Record<number, GearItem[]> => {
    // TODO
    return {};
}

export const loadRopesForDescent = (descentIds: number[]): Record<number, RopeItem[]> => {
    // TODO
    return {};
}